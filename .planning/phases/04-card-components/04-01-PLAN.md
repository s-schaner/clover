---
phase: 04-card-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/cards/ServiceCard.tsx
autonomous: true

must_haves:
  truths:
    - "Service capability cards display a geometric icon alongside a title and description"
    - "Five distinct geometric SVG icons render for cpu, cloud, code, database, shield"
    - "The animated geometric fractal hero element (GeometryAccent four-leaf clover) renders as the hero background visual"
  artifacts:
    - path: "components/cards/ServiceCard.tsx"
      provides: "Self-contained service card with geometric icon, title, description, HoverLift wrapper"
      exports: ["ServiceCard"]
  key_links:
    - from: "components/cards/ServiceCard.tsx"
      to: "lib/types.ts"
      via: "ServiceItem type import"
      pattern: "import.*ServiceItem.*from.*lib/types"
    - from: "components/cards/ServiceCard.tsx"
      to: "components/ui/HoverLift.tsx"
      via: "HoverLift wrapper"
      pattern: "import.*HoverLift.*from.*components/ui/HoverLift"
    - from: "components/cards/ServiceCard.tsx"
      to: "lib/constants.ts"
      via: "ICON_MAP keys match iconName values from SERVICES array"
      pattern: "ICON_MAP.*Record.*string.*React.ReactNode"
---

<objective>
Build the ServiceCard component with hand-crafted geometric SVG icons and verify HERO-03 is already satisfied by GeometryAccent.

Purpose: ServiceCard is the atom for the Services section (Phase 5). Each card renders a geometric icon, title, and description from typed ServiceItem props. HERO-03 requires an animated geometric fractal hero element — GeometryAccent already satisfies this; this plan verifies it.
Output: `components/cards/ServiceCard.tsx` — self-contained, typed, HoverLift-wrapped service card with 5 inline SVG geometric icons.
</objective>

<execution_context>
@C:\Users\sscha\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\sscha\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-card-components/04-RESEARCH.md
@.planning/phases/04-card-components/04-CONTEXT.md
@lib/types.ts
@lib/constants.ts
@components/ui/HoverLift.tsx
@components/ui/GeometryAccent.tsx
@app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ServiceCard with geometric SVG icon system</name>
  <files>components/cards/ServiceCard.tsx</files>
  <action>
Create `components/cards/ServiceCard.tsx` as a `'use client'` component.

**Props interface:**
```typescript
interface ServiceCardProps {
  item: ServiceItem;  // from lib/types.ts
  className?: string; // passed to HoverLift wrapper
}
```

**Icon system:** Create a local `ICON_MAP: Record<string, React.ReactNode>` with 5 hand-crafted inline SVG icons — `cpu`, `cloud`, `code`, `database`, `shield`. Each icon:
- `width="32" height="32" viewBox="0 0 24 24"` (matches Lucide grid for consistent sizing)
- `fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"`
- `aria-hidden="true"` (decorative)
- Uses geometric primitives only (rect, line, ellipse, path, polyline) — blueprint aesthetic

Icon shapes (from research):
- **cpu**: Outer rect 7,7 w10h10 + inner rect 9,9 w6h6 + 12 pin lines (3 per side) extending from outer to edges
- **cloud**: Single path arc `M18 10h-1.26A8 8 0 1 0 9 20h9a5 5 0 0 0 0-10z`
- **code**: Two polylines — `16 18 22 12 16 6` (right bracket) and `8 6 2 12 8 18` (left bracket)
- **database**: Ellipse cx12 cy5 rx9 ry3 + mid arc + path `M3 5v14c0 1.66 4 3 9 3s9-1.34 9-3V5`
- **shield**: Path `M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z`

Do NOT import from lucide-react — CONTEXT.md explicitly requires hand-crafted SVG shapes.

**Card layout:**
- Wrap entire card in `<HoverLift className={className}>` so consumers don't need to add HoverLift themselves
- Inner div with `p-[--card-padding]` (2rem), flex-col layout, gap-4
- Border: `border border-on-surface-light-subtle/20` (Services is a light section)
- No border-radius — sharp corners per design system
- `h-full` on inner div for equal-height grid support

**Rendering order:**
1. Icon (from ICON_MAP lookup via `item.iconName`, with null guard if iconName missing or not in map)
2. Title: `<h3>` with `font-display text-lg font-semibold text-on-surface-light`
3. Description: `<p>` with `font-body text-sm text-on-surface-light-muted leading-relaxed`

**Icon color:** Wrap icon in a div with `text-on-surface-light-subtle` so `currentColor` renders as muted gray on the light section background.

Export as named export: `export function ServiceCard(...)`.
  </action>
  <verify>
Run `npx tsc --noEmit` — no type errors. Verify the file exists and exports ServiceCard. Verify it imports ServiceItem from lib/types and HoverLift from components/ui/HoverLift.
  </verify>
  <done>
ServiceCard component exists at components/cards/ServiceCard.tsx. It accepts a ServiceItem prop, renders a geometric SVG icon from ICON_MAP based on item.iconName, displays title and description, and wraps in HoverLift. Five distinct icons (cpu, cloud, code, database, shield) are defined as inline SVG. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify HERO-03 — GeometryAccent already satisfies animated fractal hero element</name>
  <files>components/cards/ServiceCard.tsx</files>
  <action>
HERO-03 success criterion: "The animated geometric fractal hero element renders as an SVG-based visual component."

Verify by reading `components/ui/GeometryAccent.tsx` and `app/page.tsx`:
1. GeometryAccent is SVG-based (uses `<svg>` with `<path>`) — YES
2. GeometryAccent is animated (CSS `hero-pattern-draw` keyframe via stroke-dashoffset) — YES
3. GeometryAccent is wired in HeroSection in page.tsx (`<GeometryAccent />`) — YES
4. GeometryAccent renders a four-leaf clover fractal pattern (brand identity) — YES

No new component needed. HERO-03 is already satisfied.

Run `npm run dev` and verify the dev server starts without errors. This confirms the existing GeometryAccent and the new ServiceCard both compile correctly in the project.

Add a brief comment at the top of ServiceCard.tsx noting HERO-03 verification:
```
// Note: HERO-03 (animated geometric fractal hero element) is satisfied by
// components/ui/GeometryAccent.tsx — no separate hero card component needed.
```
  </action>
  <verify>
`npm run dev` starts without errors. GeometryAccent renders in HeroSection (already wired in page.tsx). ServiceCard compiles. No new hero component file created (intentional — GeometryAccent is sufficient).
  </verify>
  <done>
HERO-03 verified as satisfied by existing GeometryAccent component. ServiceCard created and compiles. Dev server runs clean.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `components/cards/ServiceCard.tsx` exists and exports `ServiceCard`
- ServiceCard imports `ServiceItem` from `@/lib/types`
- ServiceCard imports `HoverLift` from `@/components/ui/HoverLift`
- ICON_MAP contains exactly 5 keys: cpu, cloud, code, database, shield
- Each SVG icon uses `stroke="currentColor"` (no hardcoded colors)
- No imports from lucide-react
- No border-radius classes on card container
- GeometryAccent is already wired in page.tsx HeroSection (HERO-03 satisfied)
</verification>

<success_criteria>
1. ServiceCard renders geometric icon + title + description for any ServiceItem
2. Five distinct SVG icons map to the iconName values in SERVICES constant
3. Card wraps in HoverLift internally (consumer does not need to add HoverLift)
4. HERO-03 is verified as already satisfied (no new component needed)
5. TypeScript strict mode compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/04-card-components/04-01-SUMMARY.md`
</output>
