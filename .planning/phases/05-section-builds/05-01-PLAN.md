---
phase: 05-section-builds
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/sections/HeroSection.tsx
  - components/sections/ServicesSection.tsx
autonomous: true

must_haves:
  truths:
    - "The hero section fills the full viewport height and displays the bold positioning tagline with animated geometric fractal background"
    - "The services section lists all 5 capability areas with precise technical descriptions and geometric icon indicators per card"
  artifacts:
    - path: "components/sections/HeroSection.tsx"
      provides: "Full-viewport hero with GeometryAccent background, tagline, subheading, and CTA"
      exports: ["HeroSection"]
    - path: "components/sections/ServicesSection.tsx"
      provides: "Services grid with ServiceCard atoms, section heading with geometric accent"
      exports: ["ServicesSection"]
  key_links:
    - from: "components/sections/HeroSection.tsx"
      to: "lib/constants.ts"
      via: "imports SECTION_IDS, HERO_CONTENT"
      pattern: "import.*HERO_CONTENT.*from.*constants"
    - from: "components/sections/ServicesSection.tsx"
      to: "components/cards/ServiceCard.tsx"
      via: "renders ServiceCard for each SERVICES item"
      pattern: "<ServiceCard.*item="
---

<objective>
Build the HeroSection and ServicesSection components as proper files under `components/sections/`.

Purpose: These are the first two sections visitors see. The hero establishes the brand's positioning ("quiet confidence, engineering speaks for itself") and the services section shows what Clover Labs builds. Both are Server Components — no client-side state needed.

Output: Two new component files that compose existing Phase 3/4 primitives into fully populated, responsive sections.
</objective>

<execution_context>
@C:\Users\sscha\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\sscha\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-section-builds/05-CONTEXT.md
@.planning/phases/05-section-builds/05-RESEARCH.md
@app/page.tsx
@components/ui/SectionWrapper.tsx
@components/ui/GeometryAccent.tsx
@components/ui/GridPattern.tsx
@components/ui/FadeInOnScroll.tsx
@components/ui/StaggerChildren.tsx
@components/cards/ServiceCard.tsx
@lib/constants.ts
@lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HeroSection component</name>
  <files>components/sections/HeroSection.tsx</files>
  <action>
Create `components/sections/HeroSection.tsx` as a Server Component (no `'use client'`).

This is a near-direct extraction from the Phase 3 `page.tsx` HeroSection function. The structure is already validated:

1. Import `SectionWrapper`, `GeometryAccent`, `FadeInOnScroll`, `SECTION_IDS`, `HERO_CONTENT`.
2. Export a named function `HeroSection`.
3. Structure:
   - `SectionWrapper` with `id={SECTION_IDS.HERO}` `theme="dark"` `className="min-h-screen"`
   - Inner div: `className="relative min-h-screen flex items-center justify-center px-[--section-padding-x]"`
     - Note: This inner div intentionally keeps `px-[--section-padding-x]` because the hero does NOT use `max-w-7xl` — it needs horizontal padding for its centered flex layout. This is different from other sections.
   - `GeometryAccent` as full-bleed background (absolute positioned, needs parent `relative`)
   - Content div: `className="relative z-10 max-w-4xl text-center"`
   - Three `FadeInOnScroll` wrappers with staggered delays (0.1, 0.25, 0.4):
     - h1 with `HERO_CONTENT.tagline` — `font-display text-[length:--text-hero] leading-tight font-bold`
     - p with `HERO_CONTENT.subheading` — `font-body text-on-surface-muted mt-6 text-lg max-w-2xl mx-auto`
     - a CTA link with `HERO_CONTENT.ctaLabel` and `ctaHref` — `link-underline font-body text-on-surface mt-8 inline-block text-sm tracking-widest uppercase`

Key constraints:
- SectionWrapper does NOT add `position: relative` — the inner div must have `relative` for GeometryAccent's `absolute inset-0`.
- CTA is a text link (link-underline class), NOT a button. The tagline is the visual star.
- No GridPattern in hero — uses GeometryAccent (four-leaf clover draw animation) instead.
  </action>
  <verify>
Run `npx tsc --noEmit` — no type errors in the new file.
Confirm the file does NOT contain `'use client'` directive.
Confirm the file exports `HeroSection` as a named export.
  </verify>
  <done>HeroSection.tsx exists as a Server Component that renders full-viewport hero with GeometryAccent background, tagline, subheading, and CTA link using constants from lib/constants.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Create ServicesSection component</name>
  <files>components/sections/ServicesSection.tsx</files>
  <action>
Create `components/sections/ServicesSection.tsx` as a Server Component (no `'use client'`).

This replaces the Phase 3 placeholder ServicesSection with a fully composed section using ServiceCard atoms.

1. Import `SectionWrapper`, `GridPattern`, `FadeInOnScroll`, `StaggerChildren`, `StaggerItem`, `ServiceCard`, `SECTION_IDS`, `SERVICES`.
2. Export a named function `ServicesSection`.
3. Structure:
   - `SectionWrapper` with `id={SECTION_IDS.SERVICES}` `theme="light"`
   - Inner div: `className="relative mx-auto max-w-7xl"`
     - IMPORTANT: Do NOT add `px-[--section-padding-x]` or `py-[--section-padding-y]` — SectionWrapper already handles outer padding. The inner div only needs `relative mx-auto max-w-7xl`.
   - `GridPattern` with `theme="light"` (needs parent `relative` — provided by inner div)
   - `FadeInOnScroll` wrapping the section heading block:
     - Geometric accent element (centered, above heading):
       ```
       <div className="flex items-center justify-center gap-3 mb-6" aria-hidden="true">
         <span className="block h-px w-8 bg-current opacity-25" />
         <span className="block w-1.5 h-1.5 border border-current opacity-50 rotate-45" />
         <span className="block h-px w-8 bg-current opacity-25" />
       </div>
       ```
     - h2: `font-display text-[length:--text-section] font-bold text-on-surface-light text-center`
       Content: "Services"
     - p intro copy: `font-body text-on-surface-light-muted mt-4 max-w-2xl mx-auto text-center text-base`
       Content: "We architect and deliver at the intersection of infrastructure, AI, and custom software — engineered for production, not proof-of-concept."
     - mb-16 on the heading container div for spacing before the card grid.
   - `StaggerChildren` wrapping the card grid:
     - `className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-[--gap-grid]"`
     - Map ALL 5 `SERVICES` items (not `.slice(0, 3)` like Phase 3 did):
       ```tsx
       {SERVICES.map((service, i) => (
         <StaggerItem key={service.id} index={i} columns={3}>
           <ServiceCard item={service} className="h-full" />
         </StaggerItem>
       ))}
       ```

Key constraints:
- No `'use client'` — FadeInOnScroll and StaggerItem carry their own client boundaries.
- Render ALL 5 services (Phase 3 only rendered 3 as a validation slice).
- ServiceCard has HoverLift built in — do NOT wrap in HoverLift again.
- `h-full` on ServiceCard for equal-height cards in the grid.
- Sharp corners everywhere — no `rounded-*` utilities.
  </action>
  <verify>
Run `npx tsc --noEmit` — no type errors in the new file.
Confirm the file does NOT contain `'use client'` directive.
Confirm the file exports `ServicesSection` as a named export.
Confirm all 5 SERVICES items are mapped (no `.slice()`).
  </verify>
  <done>ServicesSection.tsx exists as a Server Component that renders a centered heading with geometric accent, intro copy, and a 3-column responsive grid of all 5 ServiceCard atoms with StaggerItem diagonal cascade animation.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors.
2. Both files exist in `components/sections/` directory.
3. Neither file contains `'use client'` — both are Server Components.
4. HeroSection imports and uses GeometryAccent (not GridPattern).
5. ServicesSection maps all 5 SERVICES items through ServiceCard atoms.
6. ServicesSection inner div does NOT duplicate padding from SectionWrapper.
7. Both files use correct dark/light theme tokens (hero = dark, services = light).
</verification>

<success_criteria>
- HeroSection.tsx and ServicesSection.tsx are valid TypeScript Server Components
- Hero renders full-viewport with GeometryAccent, tagline, subheading, and CTA
- Services renders geometric heading accent + intro copy + 5 ServiceCards in StaggerChildren grid
- No double-padding from SectionWrapper
- No double HoverLift wrapping
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-section-builds/05-01-SUMMARY.md`
</output>
