---
phase: 05-section-builds
plan: 04
type: execute
wave: 2
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - app/page.tsx
autonomous: true

must_haves:
  truths:
    - "The page renders all five sections in correct order: Hero (dark) -> Services (light) -> Portfolio (dark) -> Team (light) -> Contact (dark)"
    - "The page compiles and loads without errors in the dev server"
    - "The Phase 3 inline section functions are fully removed from page.tsx"
  artifacts:
    - path: "app/page.tsx"
      provides: "Thin orchestration file importing and rendering 5 section components"
      min_lines: 10
  key_links:
    - from: "app/page.tsx"
      to: "components/sections/HeroSection.tsx"
      via: "import and render"
      pattern: "import.*HeroSection.*from.*sections"
    - from: "app/page.tsx"
      to: "components/sections/ServicesSection.tsx"
      via: "import and render"
      pattern: "import.*ServicesSection.*from.*sections"
    - from: "app/page.tsx"
      to: "components/sections/PortfolioSection.tsx"
      via: "import and render"
      pattern: "import.*PortfolioSection.*from.*sections"
    - from: "app/page.tsx"
      to: "components/sections/TeamSection.tsx"
      via: "import and render"
      pattern: "import.*TeamSection.*from.*sections"
    - from: "app/page.tsx"
      to: "components/sections/ContactSection.tsx"
      via: "import and render"
      pattern: "import.*ContactSection.*from.*sections"
---

<objective>
Wire all five section components into page.tsx and verify the complete build.

Purpose: This is the orchestration step that transforms page.tsx from a Phase 3 validation surface into the production page composition. After this plan, the site is functionally complete — all sections rendered with real card atoms, real content, and working interactions.

Output: Updated page.tsx as a thin import-and-render file, plus verified successful build.
</objective>

<execution_context>
@C:\Users\sscha\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\sscha\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-section-builds/05-CONTEXT.md
@app/page.tsx
@.planning/phases/05-section-builds/05-01-SUMMARY.md
@.planning/phases/05-section-builds/05-02-SUMMARY.md
@.planning/phases/05-section-builds/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace page.tsx with section component imports</name>
  <files>app/page.tsx</files>
  <action>
Replace the entire contents of `app/page.tsx` with the thin orchestration file.

The current page.tsx contains:
- Phase 3 inline section functions (HeroSection, ServicesSection, PortfolioSection, TeamSection, ContactSection as local functions)
- Direct imports of UI primitives (SectionWrapper, GridPattern, etc.)
- Direct imports of constants

All of this is replaced with:

```tsx
// app/page.tsx
// Production page composition — imports 5 section components built in Phase 5.
// Dark/light alternation: Hero (dark) -> Services (light) -> Portfolio (dark) -> Team (light) -> Contact (dark)

import { HeroSection } from '@/components/sections/HeroSection';
import { ServicesSection } from '@/components/sections/ServicesSection';
import { PortfolioSection } from '@/components/sections/PortfolioSection';
import { TeamSection } from '@/components/sections/TeamSection';
import { ContactSection } from '@/components/sections/ContactSection';

export default function Home() {
  return (
    <>
      <HeroSection />
      <ServicesSection />
      <PortfolioSection />
      <TeamSection />
      <ContactSection />
    </>
  );
}
```

Key points:
- Remove ALL Phase 3 inline functions and their comments.
- Remove ALL Phase 3 imports (SectionWrapper, GridPattern, GeometryAccent, FadeInOnScroll, StaggerChildren, StaggerItem, HoverLift, constants).
- Use fragment (`<>`) not `<main>` — layout.tsx already wraps children in `<main>`.
- Section render order: Hero -> Services -> Portfolio -> Team -> Contact (matches dark/light alternation).
- No other content — page.tsx is purely orchestration.
  </action>
  <verify>
Run `npx tsc --noEmit` — no type errors.
Confirm page.tsx has exactly 5 section imports and the Home function.
Confirm NO Phase 3 inline functions remain.
Confirm NO direct imports of UI primitives or constants in page.tsx.
  </verify>
  <done>page.tsx is a clean orchestration file with 5 section imports and the Home function rendering them in order.</done>
</task>

<task type="auto">
  <name>Task 2: Verify full build succeeds</name>
  <files>app/page.tsx</files>
  <action>
Run the full Next.js production build to verify everything compiles correctly with all five sections wired together.

```bash
npm run build
```

If the build fails:
- Check for import path errors (most likely cause — verify `@/components/sections/` path alias resolves).
- Check for TypeScript errors in any of the 5 section files.
- Check for 'use client' boundary issues (only PortfolioSection should be 'use client').
- Fix any issues found and re-run.

After build passes, also run:
```bash
npm run lint
```

Fix any lint errors.
  </action>
  <verify>
`npm run build` exits with code 0.
`npm run lint` exits with code 0 (or only has pre-existing warnings).
Build output shows all routes compiled successfully.
  </verify>
  <done>Full Next.js production build passes with all 5 section components imported and rendered in page.tsx. No TypeScript, import, or lint errors.</done>
</task>

</tasks>

<verification>
1. `npm run build` exits successfully.
2. `npm run lint` exits successfully.
3. page.tsx contains exactly 5 section imports from `components/sections/`.
4. page.tsx contains NO Phase 3 inline section functions.
5. page.tsx contains NO direct imports of SectionWrapper, GridPattern, GeometryAccent, etc.
6. The Home function renders 5 section components in the correct order.
7. Build output shows the page route compiled without errors.
</verification>

<success_criteria>
- page.tsx is a thin orchestration file (~15 lines) that imports and renders 5 section components
- Full production build (npm run build) passes
- Lint (npm run lint) passes
- All Phase 3 validation artifacts are removed from page.tsx
- The site is functionally complete: all sections rendering with real card atoms and content
</success_criteria>

<output>
After completion, create `.planning/phases/05-section-builds/05-04-SUMMARY.md`
</output>
